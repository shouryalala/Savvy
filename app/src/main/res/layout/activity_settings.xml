<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                tools:context="com.catch22.savvy.SettingsActivity"
                android:id="@+id/settings_layout"
                android:padding="32dp">

    <Switch
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Show in Notifications Bar"
        android:id="@+id/notificationSwitch"
        android:checked="false"
        android:textSize="25dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"/>

    <Switch
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Access internet for service"
        android:textSize="25dp"
        android:id="@+id/accessInternetSwitch"
        android:checked="false"
        android:layout_marginTop="15dp"
        android:layout_below="@+id/notificationSwitch"
        />

</RelativeLayout>
